# === Legacy working directory (untracked, copy files from here as needed) ===
When-Algorithms-Meet-Artists-EDIT/

# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.eggs/

# === Jupyter ===
.ipynb_checkpoints/

# === Embeddings (too large for git; regenerate with src/embeddings.py) ===
embeddings/
*.npy

# Exception: canonical .npy files required by final_pipeline.py and
# referenced in CLAUDE.md as "DO NOT TOUCH" — needed for reproducibility
!figures/final_pipeline/*.npy
!figures/prefix_grid_search/*.npy
!figures/prefix_comparison/*.npy

# === OS files ===
.DS_Store
Thumbs.db

# === IDE ===
.vscode/
.idea/

# === Claude Code local config ===
.claude/
CLAUDE.md

# === Old project files (superseded by new structure) ===
mcp_server/
TRACE_template/
knowledge_bank_template/
methods_paper_template/
TRACE_data_for_talk.md

# === Manuscript working directories (private during review) ===
manuscripts_and_presentations/
nov_2025_revisions/
submissions/

# === TRACE working files (live store at ~/.trace/, not tracked here) ===
trace_provenance/sessions/
trace_provenance/_archive_pre_v03/

# === TRACE one-off normalisation scripts (used 2026-04-30 to clean up
#     pre-v0.3 sessions; not part of this project's analytical pipeline) ===
scripts/trace_normalize_*

# === Internal session-handoff and historical change logs (working notes) ===
HANDOFF_NEXT_SESSION.md
SESSION_CHANGES_*.md

# === Raw / pre-cleaning data splits (kept locally; canonical input is
#     data/public_discourse_clean_chunks.csv) ===
data/public_discourse_part1.csv
data/public_discourse_part2.csv

# === Backup / superseded data files ===
data/public_probes_embedding.csv
data/public_probes_keyword_backup.csv
data/public_probes_old_557.csv

# === Old root-level files (moved to data/ or code/) ===
artist_data.csv
better_preprocess.ipynb
Using_Sections_of_Articles.ipynb
likert_thematic_phrases.csv
